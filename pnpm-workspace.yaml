packages:
  - apps/*
  - packages/*

# Build scripts approved to run (pnpm v10+ blocks them by default).
allowBuilds:
  '@prisma/client': true
  '@prisma/engines': true
  argon2: true
  esbuild: true
  prisma: true
